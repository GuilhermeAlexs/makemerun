<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.br.makemerun.view.SubgoalsList" >

    <TextView
        android:id="@+id/txGoal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:text="Goal:"
        android:textColor="#ffffff"
        android:textSize="35sp" />

    <TextView
        android:id="@+id/txKm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/txGoal"
        android:layout_alignBottom="@+id/txGoal"
        android:layout_marginLeft="17dp"
        android:layout_toRightOf="@+id/txGoal"
        android:textSize="35sp"
        android:textColor="#ff9900"
        android:text="5km" />

    <ListView
        android:id="@+id/listSubgoals"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/txGoal"
        android:layout_below="@+id/txGoal"
        android:layout_marginTop="34dp" 
        android:dividerHeight="0dp"
		android:divider="@null">
    </ListView>

</RelativeLayout>