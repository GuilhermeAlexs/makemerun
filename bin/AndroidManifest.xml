<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.br.makemerun"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.br.makemerun.ChooseGoal"
            android:label="@string/title_activity_choose_goal" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.br.makemerun.RunTest"
            android:label="@string/title_activity_run_test" >
        </activity>
        <activity
            android:name="com.br.makemerun.MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.br.makemerun.SubgoalsList"
            android:label="@string/title_activity_subgoals" >
        </activity>

        <service
            android:name=".service.MapService"
            android:label="@string/mappingService" >
        </service>

    </application>

</manifest>
